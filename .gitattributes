# Normalize text files to LF by default.
* text=auto eol=lf

# These source directories intentionally use CRLF in working trees.
Language/** text=auto eol=crlf
Build/Skins/** text=auto eol=crlf
Build/Layouts/** text=auto eol=crlf
